# layout-spec-format — Snapshot 9

**Time:** 2026-08-08 23:30
**Kind:** message

## Events
- A23 (partial): user refined the organization — <page>.layout.txt for multi-page apps AND a single LAYOUT.txt for static websites; a better axis than scale: template-driven (static/content sites) vs screen-driven (apps); accepted with a distinctness refinement
- idea: distinctness rule — when a page within a template-driven site becomes genuinely distinct (e.g. a contact page with a form), split it into its own file; mixed usage is legal (LAYOUT.txt + contact.layout.txt side by side)
- insight: "static vs app" is really "template-driven vs screen-driven" — a static site's wireframe IS the template (content lives in the content layer); apps have genuinely distinct screens; the user's instinct validated
- insight: filename order mirrors the label-first philosophy — screen name leads; LAYOUT.<page>.txt prefix vs <page>.layout.txt suffix: equivalent globs, but the suffix keeps the human-searched name first
- arg: the one-file-one-screen invariant is relaxed to one-file-per-distinct-structure for template-driven sites, with the split-off rule preserving per-page verification where it matters
- Q25 (design tension): for template-driven sites with >1 template — one LAYOUT.txt with note-marked sections (# Template: home) vs one file per template (home.layout.txt) — open
- Q24 (unexplored): shared blocks across pages — duplicate + note convention (tentative, from earlier turn) — open

## State
### Facts & constraints
- (as Snapshot 8) LAYOUT.txt v1 locked; minimal core (15 types); boundary rule
- Organization tiers (tentative this turn): (1) static/template-driven sites → single LAYOUT.txt as the site shell; (2) multi-page apps → <page>.layout.txt flat; (3) feature-scale → folders with uniform LAYOUT.txt

### Priorities
- Simplicity / minimal grammar (anti-bloat)
- No ceremony: one file per distinct structure; the filesystem is the index (glob = inventory)
- Screen-name-first consistency (label-first philosophy extends to filenames)

### Decisions
- (all v1 decisions from Snapshot 6 remain locked; minimal core locked)
- Organization tiers (TENTATIVE this turn): static/template-driven → single LAYOUT.txt (site shell); multi-page apps → <page>.layout.txt flat; feature-scale → folders with uniform LAYOUT.txt
- Invariant: one file per distinct structure; filesystem is the index (no index/registry); glob = inventory (LAYOUT.txt / *.layout.txt / **/LAYOUT.txt) (tentative)
- Distinctness rule: a genuinely distinct page within a template-driven site splits into its own file; mixed usage legal (tentative)

### Questions
- Q1–Q22: answered (as Snapshot 8)
- Q23 (design tension): multi-page file organization — open (tiers refined; final form pending)
- Q24 (unexplored): shared blocks across pages — open (tentative: duplicate + note)
- Q25 (design tension): >1 template in template-driven sites — one LAYOUT.txt with note-marked sections vs one file per template — open

### Rejected alternatives
- (as Snapshot 8)
- LAYOUT.<page>.txt prefix naming (suffix keeps the human-searched name first)

### Next steps
- User decides Q25 and finalizes Q23; then fold catalog + organization into the final spec document
