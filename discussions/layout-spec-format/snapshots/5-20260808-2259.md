# layout-spec-format — Snapshot 5

**Time:** 2026-08-08 22:59
**Kind:** message

## Events
- A12: note marker = # (locked)
- A14: widget vocabulary = FREE-FORM convention, NOT validated against DESIGN.md — significant: verification becomes structure-only; vocabulary is a soft convention; layout-first workflow is a legitimate reason (sketches often lead the design system)
- correction: user overrode my validation recommendation — free-form vocabulary over DESIGN.md validation; apply going forward
- A8: variant syntax = colon [x](type:variant) — my recommendation, user accepted; space-form is incompatible with free-form vocabulary (ambiguous parsing), separate-bracket adds a third grammar shape
- idea: first-token parse rule — type = first token, optional :variant after the first colon, rest of the parens = opaque passthrough attributes ([Docs](link https://example.com) falls out with zero extra grammar)
- arg: space-separated variants would reintroduce a closed vocabulary through the back door (ambiguous parse: type "button primary" vs type "button" + variant) — incompatible with the free-form choice
- Q15 (design tension): repetition placement — 3x (card) vs (card) x3 — open (asked this turn)
- Q19 (design tension): file organization — one LAYOUT.txt per screen/feature vs a single file — open (asked this turn)
- Q16 (unexplored): container variants — (card:compact) via the same colon rule — likely yes, unconfirmed — open
- Q18 (design tension): verification mechanics detail — structure-only checks (presence, order, hierarchy, labels) + informational vocabulary report — direction set — open
- Q17 (unexplored): real links as widgets — works via the passthrough rule (tentative) — open
- Q20 (unexplored): multi-screen — how multiple screens/features relate in files — open
- insight: the vocabulary decision constrains the syntax decision — free-form ⇒ colon variants
- insight: two shapes ([label](type), (type)) + first-token rule + # notes + indentation covers the wireframe scope; grammar now ~7 rules
- insight: verification = structural (what/where/how many); vocabulary = soft convention

## State
### Facts & constraints
- (as Snapshot 4) LAYOUT.txt locked; consumer/scope locked; no code fences; notes middle path
- # LOCKED as note marker; widget vocabulary FREE-FORM (not validated); colon variants; first-token parse rule

### Priorities
- Simplicity / minimal grammar (anti-bloat)
- Natural reading for humans; deterministic parseability for agents
- Vocabulary freedom: layout-first workflow — sketches lead the design system (new, user-driven)
- Lenient for humans, deterministic for agents

### Decisions
- LAYOUT.txt — LOCKED (plain text, own grammar, no markdown semantics)
- DESIGN.md stays for visuals — LOCKED
- Consumer: humans + agents (implement & verify) — LOCKED
- Scope: wireframe-level structure only, no behavior — LOCKED
- Containers: (type) line + indented children (option B) — LOCKED
- Leaf markers: [label](type) — LOCKED
- Notes: # prefix — LOCKED
- Widget vocabulary: FREE-FORM convention — LOCKED (not validated against DESIGN.md; verification checks structure only; unknown types may be reported as informational)
- Variant syntax: colon [x](type:variant) — LOCKED (my recommendation, user accepted)
- First-token parsing rule: type = first token, optional :variant, remaining parens content = passthrough attributes (e.g. [Docs](link https://example.com)) — LOCKED
- Same-line = row — tentative
- xN repetition — tentative (placement undecided)

### Questions
- Q1 (value judgment): filename/format — answered: LAYOUT.txt
- Q2 (design tension): marker disambiguation — answered: moot in .txt
- Q3 (design tension): grammar scope — open (largely resolved by the locked two-shape grammar; convergence pending)
- Q4 (value judgment): behavior in scope — answered: no behavior
- Q5 (value judgment): primary consumer — answered: both
- Q6 (design tension): container/nesting mechanism — answered: (type) line + indented children
- Q7 (unexplored): repetition syntax — open (xN sketch, placement undecided)
- Q8 (unexplored): variant/state syntax — answered: colon [x](type:variant)
- Q9 (design tension): grammar density — answered: middle path — # notes
- Q10 (unexplored): ASCII sketch blocks — open
- Q11 (unexplored): type-first vs label-first — open
- Q12 (value judgment): note marker character — answered: #
- Q13 (unexplored): notes attachment position rule — open
- Q14 (design tension): vocabulary validation vs free-form — answered: free-form convention
- Q15 (design tension): repetition placement — 3x (card) vs (card) x3 — open
- Q16 (unexplored): container variants ((card:compact)) — open (likely yes via colon rule)
- Q17 (unexplored): real links as widget type — open (works via passthrough, tentative)
- Q18 (design tension): verification mechanics detail — open (direction set: structure-only + informational vocab report)
- Q19 (design tension): file organization — per-screen vs single file — open
- Q20 (unexplored): multi-screen file relations — open

### Rejected alternatives
- Code fences as container mechanism (user rejected)
- Fully lenient parsing (user's middle path rejected it)
- Vocabulary validated against DESIGN.md (user overrode my recommendation)
- Space-separated variants (ambiguous parse, incompatible with free-form)
- Separate-bracket variant syntax (third grammar shape)

### Next steps
- Repetition placement (Q15), file organization (Q19), container variants (Q16), verification detail (Q18)
