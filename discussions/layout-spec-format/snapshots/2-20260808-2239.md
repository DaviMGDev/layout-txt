# layout-spec-format — Snapshot 2

**Time:** 2026-08-08 22:39
**Kind:** message

## Events
- A5: consumer = BOTH — humans read, agents implement & verify — the format needs deterministic structure, not just prose
- A4: scope = "whatever makes sense to be present in a prototype/wireframe" — widgets, arrangement, hierarchy, labels, variants/states, annotations; NOT events/navigation/data logic
- insight: the format defines NO vocabulary — widget types reference DESIGN.md's components ([Save](button:primary) names a token that already exists); verification falls out: unknown marker type = spec error, built DOM must match markers — a referencing layer, not a self-contained language
- insight: incompleteness as a feature — escape hatches ("if the format fights you, write a prose sentence") are OK because humans fill holes; the real anti-bloat mechanism
- insight: same-line = row, new-line = column is a natural ordering rule; markdown tables can express grids natively
- insight: containers are unavoidable — multi-widget repetition (grid of cards) requires named groups
- Q6 (design tension): container/nesting mechanism — fence blocks, indented trees, inline brackets, or none — open
- Q7 (unexplored): repetition syntax (x3 / list items) — open
- Q8 (unexplored): variant/state syntax ([label](type:variant) vs [label](type disabled)) — open
- Q18 (design tension): verification mechanics — how agents verify against the layout file; candidate: marker types must exist in DESIGN.md components; DOM must match presence/order/hierarchy — open
- Q19 (unexplored): per-screen files vs one file — open

## State
### Facts & constraints
- (as Snapshot 1) InterSpec → simple text format; DESIGN.md owns visuals/tokens; [label](type) seed syntax
- Consumer LOCKED: humans read + agents implement AND verify
- Scope LOCKED: wireframe/prototype level — widgets, hierarchy, order, labels, variants/states, annotations; no behavioral programming semantics
- Verification requirement ⇒ the format must be deterministically parseable

### Priorities
- Simplicity / minimal grammar (anti-bloat)
- Natural reading for humans; deterministic parseability for agents (new — forced by verification)
- Design-system first: the layout layer references DESIGN.md, never redefines it

### Decisions
- Replace InterSpec as the structure layer with a simple text wireframe format (tentative)
- Keep DESIGN.md for visuals/tokens (confirmed by user's setup)
- Markdown-link-style inline markers [label](type) as the seed syntax (tentative)
- Consumer: humans read + agents implement AND verify (locked)
- Scope: wireframe/prototype level — no behavioral semantics (locked)

### Questions
- Q1 (value judgment): filename/format — LAYOUT.md vs STRUCTURE.md vs LAYOUT.txt (lean: LAYOUT.md or WIREFRAME.md) — open
- Q2 (design tension): marker disambiguation from real markdown links — open
- Q3 (design tension): grammar scope — nesting/repetition/variants in linear text — open
- Q4 (value judgment): behavior in scope, or pure static structure — answered: wireframe scope only, no behavior
- Q5 (value judgment): primary consumer — answered: both — humans read, agents implement & verify
- Q6 (design tension): container/nesting mechanism — open
- Q7 (unexplored): repetition syntax — open
- Q8 (unexplored): variant/state syntax — open
- Q18 (design tension): verification mechanics — open (candidate: types reference DESIGN.md components; DOM matches presence/order/hierarchy)
- Q19 (unexplored): per-screen files vs one file — open

### Rejected alternatives
- None yet

### Next steps
- Pick the container mechanism (Q6); repetition (Q7) and variants (Q8) follow the container shape
