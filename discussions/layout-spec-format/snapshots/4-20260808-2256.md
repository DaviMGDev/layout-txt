# layout-spec-format — Snapshot 4

**Time:** 2026-08-08 22:56
**Kind:** message

## Events
- A1: file home locked — LAYOUT.txt (plain text, own grammar, no markdown semantics)
- correction: notes need at least a minimal identifying mark — user's middle path between fully lenient and fully strict ("prose is supposed to be used to help, add at least something to identify each one"); notes also serve the implementer
- idea: notes carry a minimal identifying mark (e.g. # prefix) — marker char undecided
- Q12 (value judgment): note marker character — # vs ; vs // vs other — open
- Q13 (unexplored): notes attachment — position rule (note directly above a widget = refers to it)? — open
- Q14 (design tension): vocabulary validation — marker types validated against DESIGN.md components (unknown = warning/error) vs free-form convention — open
- Q15 (design tension): repetition placement — 3x (card) prefix vs (card) x3 suffix — open
- Q16 (unexplored): container variants ((card:compact)) — symmetry leaf=[] group=() — open
- Q17 (unexplored): real links as widget type ([Docs](link https://…)) — open
- insight: the two-shape grammar (leaf [label](type) / group (type)) may be sufficient for wireframe scope
- insight: strict-enough for agents, loose enough for humans — a single-char marker keeps the file parseable without ceremony

## State
### Facts & constraints
- (as Snapshot 3) Consumer and scope locked; .txt direction; no code fences
- LAYOUT.txt LOCKED as the file home — plain text, no markdown semantics
- Notes: middle path — marked, not fully lenient; marker char undecided

### Priorities
- Simplicity / minimal grammar (anti-bloat)
- Natural reading for humans; deterministic parseability for agents
- Incompleteness-as-feature: escape hatches over grammar growth

### Decisions
- Replace InterSpec as the structure layer with LAYOUT.txt — LOCKED (plain text, no markdown semantics)
- Keep DESIGN.md for visuals/tokens — LOCKED
- Consumer: humans read + agents implement AND verify — LOCKED
- Scope: wireframe-level structure (widgets, hierarchy, order, labels, variants/states, annotations); no behavior — LOCKED
- Code fences rejected — LOCKED
- Containers: indented trees, (name) marker line + indented children (tentative, option B)
- Markers: [label](type) inline (tentative)
- Notes: carry a minimal identifying mark (e.g. # prefix) — user's middle path (tentative, marker char undecided)

### Questions
- Q1 (value judgment): filename/format — answered: LAYOUT.txt
- Q2 (design tension): marker disambiguation from real markdown links — answered: moot — .txt has no markdown link semantics
- Q3 (design tension): grammar scope — nesting/repetition/variants in linear text — open
- Q4 (value judgment): behavior in scope — answered: wireframe scope only, no behavior
- Q5 (value judgment): primary consumer — answered: both — humans read, agents implement & verify
- Q6 (design tension): container/nesting mechanism — open (indented trees tentative)
- Q7 (unexplored): repetition syntax — open
- Q8 (unexplored): variant/state syntax — open
- Q9 (design tension): grammar density — strict vs lenient — answered: middle path — notes carry a minimal identifying mark
- Q10 (unexplored): ASCII sketch blocks as annotation — open
- Q11 (unexplored): type-first vs label-first syntax — open
- Q12 (value judgment): note marker character — open
- Q13 (unexplored): notes attachment position rule — open
- Q14 (design tension): vocabulary validation vs free-form — open
- Q15 (design tension): repetition placement — open
- Q16 (unexplored): container variants — open
- Q17 (unexplored): real links as widget type — open
- Q18 (design tension): verification mechanics — open
- Q19 (unexplored): per-screen files vs one file — open

### Rejected alternatives
- Code fences as container mechanism (user rejected)
- Fully lenient parsing — unmatched lines are implicit notes with no marker (user's middle path rejected it)

### Next steps
- Note marker char (Q12), vocabulary stance (Q14), variant syntax (Q8), repetition (Q7/Q15)
