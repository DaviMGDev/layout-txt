# layout-spec-format — Snapshot 3

**Time:** 2026-08-08 22:42
**Kind:** message

## Events
- correction: ".md required?" — user: "we really need to use .md for specification formats? why not use txt for this one? I don't like using code blocks for this" — the .md default assumption is corrected; user prefers the .txt direction
- correction: code fences rejected as a container mechanism (option A)
- idea: indented trees (option B) tentatively preferred ("maybe I liked B?"); user asks for more suggestions and considerations
- insight: the user's two objections (code fences; markdown link collision) BOTH evaporate in .txt — the preference is coherent, not random: a format that defines everything itself, inherits nothing
- insight: the extension decides what is inherited — .md = inherit markdown semantics + rendering, fight collisions and fence noise; .txt = define 100% of the grammar, gain monospace freedom (ASCII sketches become legal), lose rendering
- insight: lenient parsing — "anything that isn't a widget marker is a note" — removes the need for any comment syntax (#, //); Fountain precedent (unmatched lines = action/description); typos silently become notes (cost of leniency, but aligns with incompleteness-as-feature)
- arg: the .md argument is mostly ecosystem inertia + heading rendering; DESIGN.md/MKF/OKF are markdown because they're documents/token files, not structure specs
- Q9 (design tension): grammar density — strict (all prose must be marked) vs lenient (unmatched lines = notes) — open
- Q10 (unexplored): ASCII sketch blocks as allowed annotation — possible txt-only superpower — open
- Q11 (unexplored): type-first syntax (input: Email) vs label-first ([Email](input)) — open

## State
### Facts & constraints
- (as Snapshot 2) Consumer and scope locked; verification ⇒ deterministic parsing
- .txt direction: no markdown semantics, no fences, no rendering — format defines 100% of its own grammar
- Code fences are off the table for containers

### Priorities
- Simplicity / minimal grammar (anti-bloat)
- Natural reading for humans; deterministic parseability for agents
- The format inherits nothing: .txt defines its own semantics (new, user-driven)

### Decisions
- Replace InterSpec as the structure layer with a simple text wireframe format (tentative)
- Keep DESIGN.md for visuals/tokens (confirmed)
- Consumer: humans read + agents implement AND verify (locked)
- Scope: wireframe/prototype level, no behavioral semantics (locked)
- Code fences (option A containers) REJECTED by user (locked)
- Indented trees (option B) tentatively preferred for containers
- .md is being questioned — user prefers the .txt direction (locked)
- [label](type) markers: still tentative, syntax open

### Questions
- Q1 (value judgment): filename/format — LAYOUT.md vs STRUCTURE.md vs LAYOUT.txt — open (leaning .txt)
- Q2 (design tension): marker disambiguation from real markdown links — open (moot if .txt — one of the strongest arguments FOR txt)
- Q3 (design tension): grammar scope — nesting/repetition/variants in linear text — open
- Q4 (value judgment): behavior in scope — answered: wireframe scope only, no behavior
- Q5 (value judgment): primary consumer — answered: both — humans read, agents implement & verify
- Q6 (design tension): container/nesting mechanism — open (indented trees tentatively preferred; exact syntax within trees still open)
- Q7 (unexplored): repetition syntax — open
- Q8 (unexplored): variant/state syntax — open
- Q9 (design tension): grammar density — strict vs lenient — open
- Q10 (unexplored): ASCII sketch blocks as annotation — open
- Q11 (unexplored): type-first vs label-first syntax — open
- Q18 (design tension): verification mechanics — open
- Q19 (unexplored): per-screen files vs one file — open

### Rejected alternatives
- Code fences as container mechanism (user rejected)
- Markdown as the file home (questioned; user prefers .txt)

### Next steps
- Settle grammar density (Q9), container syntax within trees (Q6), and the .txt naming (Q1)
