# layout-spec-format — Snapshot

**Time:** 2026-08-08 22:56

## Since last snapshot
User locked in LAYOUT.txt as the file home. On the parsing stance they pushed a middle path: "prose is supposed to be used to help, add at least something to identify each one" — notes should carry at least a minimal identifying mark (not fully lenient, not fully strict). Also implicitly: notes serve the implementer.

## Current state
## Active decisions
- Replace InterSpec as structure layer with LAYOUT.txt — LOCKED (plain text, no markdown semantics).
- Keep DESIGN.md for visuals/tokens — LOCKED.
- Consumer: humans read + agents implement AND verify — LOCKED.
- Scope: wireframe-level structure (widgets, hierarchy, order, labels, variants/states, annotations). No behavior — LOCKED.
- Code fences rejected — LOCKED.
- Containers: indented trees, `(name)` marker line + indented children (tentative, option B).
- Markers: `[label](type)` inline (tentative).
- Notes: carry a minimal identifying mark (e.g. # prefix) — user's middle path between lenient and strict (tentative, marker char undecided).

## Open questions
- Note marker character: # vs ; vs // vs other (value judgment — asked this turn).
- Notes attachment: position rule (note directly above widget = refers to it)? (unexplored)
- Vocabulary validation: should marker types be validated against DESIGN.md components (unknown type = warning/error) or free-form convention? (design tension — asked this turn)
- Variant syntax: [label](type:variant) vs [label](type variant) vs other (asked this turn).
- Repetition: `xN` suffix, placement (3x (card) vs (card) x3) (tentative sketch).
- Same-line = row rule (tentative sketch).
- Real links as widget type: [Docs](link https://…) (unexplored).
- File organization: one LAYOUT.txt per screen/feature vs single file (unexplored).
- Verification mechanics: DOM matches markers in order/hierarchy; types resolve to DESIGN.md (tentative direction).
- Container variants: (card:compact)? Symmetry leaf=[] group=() (unexplored).

## Reasoning chains
- User's middle path on notes: prose exists to help the implementer, but must be identifiable as prose — a single-char marker keeps the file parseable without ceremony. Strict-enough for agents, loose enough for humans.
- .txt removes markdown collisions and fence noise; format defines 100% of its semantics.
- Elegant core: two shapes — [label](type) leaf, (type) group; everything else is commentary if marked.
- Incompleteness-as-feature; lenient-for-humans, strict-for-agents verification split.

## Key insights
- Extension decision is really "what do you inherit": .txt inherits nothing, defines everything.
- Two-shape grammar (leaf/group) may be sufficient for wireframe scope.

## User corrections
- "We really need to use .md?" — prefers .txt; md default corrected.
- Rejects code fences.
- Notes need a minimal identifier (not fully lenient).
