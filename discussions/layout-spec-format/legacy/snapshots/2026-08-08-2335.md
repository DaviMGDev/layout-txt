# layout-spec-format — Snapshot

**Time:** 2026-08-08 23:35

## Since last snapshot
User simplified my template concept away (found it confusing — correct): FINAL organization model — (1) static websites: one single LAYOUT.txt; (2) multi-page apps: &lt;page&gt;.layout.txt flat; (3) if the app directory gets crowded: subfolders by domain, but files KEEP the &lt;page&gt;.layout.txt pattern (auth/login.layout.txt, auth/signup.layout.txt). Screen name is always the file stem — folders are pure grouping, never rename the file.

## Current state
## Active decisions (locked — organization finalized)
- LAYOUT.txt v1 grammar/verifier as exported.
- Vocabulary: free-form + minimal universal core (15 types).
- FILE ORGANIZATION (final, user's model): static website → single LAYOUT.txt; multi-page app → &lt;page&gt;.layout.txt flat; crowded app dir → domain subfolders keeping &lt;page&gt;.layout.txt naming (auth/login.layout.txt). Screen name = file stem always (folders never rename files). No index/registry; glob = inventory (**/*.layout.txt); cross-references by stem.

## Open questions
- None structural. Remaining: exact composition of the 15-type core list (draft proposed, adjustable); shared-block duplication convention (duplicate + note — tentative); fold catalog + organization decisions into the final spec document.

## Reasoning chains
- User's model beats the template concept: fewer concepts, screen name always in filename, folders = pure grouping, cross-refs by stem uniformly. Template-driven vs screen-driven distinction collapsed into "static → one file; app → per-page" without losing anything (content-driven sites genuinely have one shell).
- Consistency: label-first philosophy extends to filenames (page name leads, .layout.txt follows) and survives subfolder grouping.

## Key insights
- Screen identity lives in the file stem, never the folder path.
- Simplification wins: the user's two-rule model replaced my three-tier + template idea.
