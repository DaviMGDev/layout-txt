# layout-txt-writer-skill — Snapshot 2

**Time:** 2026-08-09 00:05
**Kind:** manual

## Events
insight: pi auto-discovers project skills from .pi/skills/ and .agents/skills/ (committed, cross-harness standard); a top-level skills/ dir is NOT auto-loaded
decision (mine): canonical skill lives in versioned skills/layout-txt/; loading via committed symlink .agents/skills/layout-txt -> ../../skills/layout-txt (fallback: .pi/settings.json skills array pointing at ../skills)
insight: agentskills.io standard — .agents/skills/ is the agent-agnostic shared location, so a committed symlink survives fresh clones

## State
### Facts & constraints
- Repo: /home/davi/Projects/local/web — LAYOUT.txt format spec project (specs/SPEC.md + specs/LANGUAGE.md, v1, status draft)
- LAYOUT.txt: plain-text wireframe structure format; 5 constructs ([Label](type:variant attrs), (type) groups + indentation, xN repetition, # notes, same-line=row); free-form vocabulary with 15-type recommended core; verification is structural (out of scope here)
- Existing conventions: MKF compound nodes (index.md + log.md), spec-md style, conventional commits, discussion skill ecosystem, skill-creator skill available (validate.py, scaffold.py)
- Pi skill discovery (from pi docs): global ~/.pi/agent/skills, ~/.agents/skills; project .pi/skills/ and .agents/skills/ (trusted); settings skills array; CLI --skill. Root-level skills/ NOT auto-loaded. .agents/skills/ ignores root .md but discovers dirs with SKILL.md recursively.
- .gitignore currently ignores only .pi/ and .pi-subagents/ — .agents/ is committable
- User flow: thinker helper → full proposal → user opines only at the end
### Priorities
- Proposal quality and completeness; skill must be genuinely useful for agents writing LAYOUT files
- Consistency with the project's own spec conventions (single source of truth, index.md registries)
- Small, focused skill — matches LAYOUT.txt's own anti-bloat philosophy
### Decisions
- Canonical skill in versioned top-level skills/layout-txt/
- Loading via committed symlink .agents/skills/layout-txt → ../../skills/layout-txt (pi project discovery); fallback documented: .pi/settings.json {"skills": ["../skills"]}
### Questions
- (open, to be resolved by me + thinker) skill name (leaning layout-txt), embed grammar vs reference specs, skill contents/structure, include a parse-check script or not, skills/ directory conventions (index.md/log.md), commit strategy
### Rejected alternatives
- skills/ under .pi/ only: hidden, gitignored, not shareable — rejected (user asked for visible skills/)
### Next steps
- Manual snapshot done — now brief the thinker (self-contained: full grammar + design questions; ask for proposal draft + stress test)
- Synthesize thinker output + my resolutions into the full proposal
- Present proposal; user gives opinion
